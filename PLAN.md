# PLAN — ストップウォッチ

## 1. 概要

開始・停止・リセットのボタンで経過時間を1/100秒単位で計測し、ラップボタンでその時点の経過時間を一覧に記録できる静的単一ページWebアプリを作る。バニラJSのみで `public/index.html` 1ファイルに完結させ、Cloudflare Workers assets で配信する。

## 2. 意図（明示）

プレゼン練習や筋トレなどのトレーニング中に、開きっぱなしにして使う計測道具。
ラップはセクション・セットごとの所要時間を後で振り返るための記録。

## 3. 受け入れ条件

各条件は `tests/app.spec.ts` の Playwright テストで機械的に合否判定する。判定に使う観測点を各条件に併記する。

- [ ] 初期表示では経過時間表示が `00:00.00`、ラップ一覧が空、停止状態（開始操作が可能）になっている
- [ ] 「開始」ボタンを押すと経過時間が1/100秒単位でカウントアップし始める（判定: 押下後に表示値が `00:00.00` から増加し、表示が `MM:SS.cc` 形式＝1/100秒の桁まで含む）
- [ ] 計測中に「停止」ボタンを押すとカウントアップが止まる（判定: 停止後に時間を進めても表示値が変化しない）
- [ ] 停止後に再度「開始」を押すと停止時点の値から再開する（判定: 再開直後の表示値が停止時の表示値と一致し、その後増加する）
- [ ] 計測中に「ラップ」ボタンを押すと、その時点の経過時間がラップ番号付きで一覧に追加される（判定: 追加された行が押下時点の表示値と一致し、複数回押すと行数がその回数だけ増える）
- [ ] 「リセット」ボタンを押すと経過時間表示が `00:00.00` に戻り、ラップ記録もすべてクリアされ、停止状態になる（計測中・停止中のどちらから押しても同じ結果）

## 4. 実装方針

`public/index.html` 単一ファイル（CSS/JSインライン、ビルドなし）で実装する。

### 技術

- **時間計測**: `performance.now()` を基準にした差分計算。加算式カウンタは使わない（タイマー遅延で誤差が蓄積するため）。状態は「停止までの累積時間 `elapsedBefore`」と「走行開始時刻 `startTime`」の2つを分けて持ち、表示値は `elapsedBefore + (now - startTime)` で常に導出する
- **表示更新**: `setInterval`（約16ms間隔）で表示のみを更新。更新が遅れても計測値は正確
- **状態**: `running`（真偽値）、`elapsedBefore`、`startTime`、`laps`（配列）。永続化はしない（開きっぱなし利用が前提のため）
- **表示形式の境界**: `MM:SS.cc` の分は 60 分以上でも桁を増やしてそのまま表示する（例: `100:05.30`）。時への繰り上げは行わない
- **操作の境界**: ラップボタンは計測中のみ有効（停止中は disabled）。リセットは計測中・停止中いずれでも有効

### レイアウト

- 中央寄せの縦一列構成: 経過時間の大型表示（等幅フォント）→ ボタン列（開始/停止・ラップ・リセット）→ ラップ一覧（番号 + 経過時間、新しいものが上）
- 開始/停止は1つのトグルボタンにし、状態に応じてラベルと色を切り替える。ラップは計測中のみ有効化
- ダーク基調のシンプルなテーマ。favicon はストップウォッチ絵柄のインライン SVG data URI
- フッターは AGENTS.md 固定マークアップで `apps.jozo.beer` への導線を最下部に配置（body がセンタリングレイアウトになる場合は flex-direction: column にして崩れを防ぐ）

### 主要関数

| 関数 | 役割 |
|---|---|
| `start()` | `startTime = performance.now()` を記録し、表示更新タイマーを開始 |
| `stop()` | 経過分を `elapsedBefore` に積んでタイマーを停止 |
| `reset()` | 全状態を初期化し、表示を `00:00.00` に戻しラップ一覧を空にする |
| `lap()` | 現在の経過時間を `laps` に追加し一覧を再描画 |
| `getElapsed()` | 現在の経過ミリ秒を導出（running 中は `now - startTime` を加算） |
| `formatTime(ms)` | ミリ秒を `MM:SS.cc` 文字列に変換 |
| `render()` | 経過時間表示・ボタン状態・ラップ一覧を状態から描画 |

### テスト

受け入れ条件それぞれに対応する Playwright テストを1本以上 `tests/app.spec.ts` に追記する（雛形のスモークテストは残す）。時間経過の検証は `page.clock` で仮想時計を進めて表示値を決定的に判定することを第一候補とし、使えない場合のみ `expect.poll` 等のポーリングで「増加した／変化しない」を判定する。完成条件は AGENTS.md のとおり `npm run verify` と `npm test` の両方が通ること。
